# Core
Django==6.0.5
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0

# ASGI + Channels (for WebSocket console in Phase 1)
channels[daphne]==4.3.2

# Static files / SPA serving in prod
whitenoise==6.12.0

# Secrets / hashing
argon2-cffi==23.1.0
pyotp==2.9.0

# Phase 1 (Minecraft container management)
docker==7.1.0
# RCON is implemented in stdlib (server/rcon.py) — no third-party dep needed.

# Cron scheduling (schedules app)
croniter==6.2.2

# S3-compatible off-site backup sync (Phase 1.6b)
boto3==1.43.9

# Server-icon validation + preset generation (Phase 1.7d)
Pillow==11.0.0

# Plugin metadata (plugin.yml, paper-plugin.yml) for the manual upload flow
PyYAML==6.0.3

# Server
gunicorn==26.0.0
